# Vanquish — client viewers

Static, self-contained interactive 3D packaging reviews, served via GitHub Pages.

- `stky-v3/` — STKY V3 retail cartons (single, 10 pack, 100-pack master box)

Each viewer is a single HTML file with geometry and textures inlined, so it needs no build step and no network beyond fonts.
